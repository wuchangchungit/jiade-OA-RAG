# Cursor AI Agent 多轮对话系统开发提示词

你是一个资深的AI全栈技术专家、架构师。请你严格按照以下步骤和规范，协助我逐步开发“基于API KEY的AI Agent多轮对话系统”。为了配合我基于Vibe Coding的开发习惯，请确保代码结构的模块化和可维护性。

## 核心交互原则与约束（非常重要）
1. **分阶段生成（严格执行）**：你必须严格按照下述的“开发阶段（Phase 1-5）”逐步生成代码。每次只输出当前阶段的代码或配置文件。完成当前阶段后，**必须停止输出，并询问我**：“该阶段代码已生成，请核对。是否确认并继续生成下一阶段代码？”。在没有得到我明确同意之前，绝不可擅自提前输出后续阶段的代码。
2. **代码规范**：所有代码必须包含清晰、详尽的中文注解，方便后续审查与修改。具备完善的日志(logging)功能，方便后台排查问题。
3. **环境基准**：开发与运行指南需基于本地的Conda环境，环境名称为 `test`。在项目初始化阶段，必须生成 `.env.example` 文件。
4. **绝对约束**：全文及代码注释中**严格禁用任何emoji表情符号**。

---

## 阶段一：基础架构与容器化环境准备
**任务目标**：搭建项目骨架，配置数据库与监控组件。
**具体要求**：
- 创建项目基础目录结构，在项目根目录创建名为 `document` 的文件夹，用于存放测试数据。
- 编写 `docker-compose.yml`，要求一键拉起 Langfuse（包含其自身依赖的DB）以及项目业务所需的 Postgres 数据库。
- 生成包含所有环境变量模板的 `.env.example` 文件（包含 API KEY、数据库连接串、Langfuse 密钥等）。
- 生成基于 conda `test` 环境的 `requirements.txt`。
*(当前阶段完成后请暂停，等待我确认)*

## 阶段二：RAG核心模块与LangGraph工具封装
**任务目标**：基于LlamaIndex构建高级检索模块，并封装为LangGraph工具。
**具体要求**：
- **框架选择**：使用 LlamaIndex 进行文档加载与处理。
- **文档分片**：实现“父子块分片优化”（HierarchicalNodeParser / AutoMergingRetriever）。
- **向量数据库**：集成 Chroma 作为底层存储。
- **高级检索**：实现混合检索（Hybrid Search），并在检索后加入重排序（Reranking）逻辑。
- **工具封装**：将上述 RAG 检索流程完整封装为一个工具函数或类，命名必须为 `ems_handbook_tool`，并符合 LangGraph Tool 的调用规范。
*(当前阶段完成后请暂停，等待我确认)*

## 阶段三：后端核心API开发
**任务目标**：基于FastAPI构建后端逻辑与多轮对话图结构。
**具体要求**：
- **框架使用**：后端完全使用 FastAPI 构建，采用前后端分离的设计思路提供 API 接口。
- **图工作流**：使用 LangGraph 框架构建 Agent 工作流，将上一步的 `ems_handbook_tool` 集成到图中。
- **多轮对话机制**：必须采用异步（Async）、流式（Streaming）的方式与大语言模型交互，将生成结果流式返回给前端。
- **认证模块**：开发基于用户名和密码的登录接口，并实现图形验证码生成逻辑（要求验证码图片或SVG清晰可见）。
- **文档处理**：实现后端接口，接收前端上传的 Word 或 Markdown 文档，若是 Word 文档必须包含自动将其转换为 Markdown 格式的处理逻辑，并保存入库/加入索引。
*(当前阶段完成后请暂停，等待我确认)*

## 阶段四：前端Web界面与交互逻辑开发
**任务目标**：构建美观大方且功能完善的交互界面。
**具体要求**：
- **基础技术**：前端使用纯 HTML/CSS/JS 或轻量级框架（如 Vue3/Tailwind，由你根据最佳实践推荐并生成），通过 FastAPI 挂载静态页面或模板。
- **UI 设计感**：界面设计必须美观大方，具备现代感和专业感，采用合理的留白和配色。
- **界面元素规范**：
  - 页面标题/头部必须包含字样："上海佳得森辉新材料(集团）有限公司RAG 问答 Demo（作者：吴常春）"。
  - 登录页：提供用户名、密码输入框及验证码展示与校验。
  - 主工作区采用左右分栏布局：
    - 左侧栏：放置“上传文件”按钮与文件列表，支持选择 Word 和 Markdown 文档上传。
    - 右侧栏：放置核心的多轮对话界面，支持实时渲染后端返回的流式 Markdown 数据。
*(当前阶段完成后请暂停，等待我确认)*

## 阶段五：Nginx 反向代理与 HTTPS 配置
**任务目标**：生成生产环境相关的代理与证书配置指导。
**具体要求**：
- 提供 Nginx 的配置文件示例（`nginx.conf`），实现对 FastAPI 后端服务的反向代理。
- 代理配置中需包含 HTTPS 支持。
- 附带一份关于如何“手工创建/自签发SSL证书”的命令行指令与步骤说明，以及如何将证书配置到 Nginx 中的指南。
*(项目全部阶段完成)*

请阅读并理解上述所有要求。如果理解无误，请直接开始输出【阶段一】的内容。